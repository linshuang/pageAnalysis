package bit.lin.ad;

public class ADExtractor {
	// w is the threshold for Density
	private final double w = 0.5;
	// default font
	private final double font = 13;

	public void process() {
//		if(has no child){
//			remark text;
//		}
		if (canBeProcessedByA()) {
			/*
			 * Process By Area
			 */
//			double sum =0.0;
//			for(all children in node){
//				sum+=child.area;
//			}
//			for(all children in node){
//				if(area/sum>=w1){
//					for(all children in child)
//						process();
//					break;
//				}
//			}
		} else {
			/*
			 * Process By Density
			 */
		}
	}

	private boolean canBeProcessedByA() {
		return false;
	}
}
